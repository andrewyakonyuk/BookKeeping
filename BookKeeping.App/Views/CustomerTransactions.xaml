<UserControl x:Class="BookKeeping.App.Views.CustomerTransactions"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:viewmodels="clr-namespace:BookKeeping.App.ViewModels"
             d:DesignHeight="300" d:DesignWidth="300" >
    <UserControl.Resources>
        <ResourceDictionary>
            
        </ResourceDictionary>
    </UserControl.Resources>
    <DataGrid ItemsSource="{Binding Path=Source}" IsReadOnly="True" Style="{DynamicResource AzureDataGrid}" AutoGenerateColumns="False">
        <DataGrid.Columns>
            <DataGridTextColumn Binding="{Binding Name}" Header="Name" Width="*" MaxWidth="200" MinWidth="100"></DataGridTextColumn>
            <DataGridTextColumn Binding="{Binding Change}" Header="Change" Width="*" MaxWidth="200" MinWidth="100"></DataGridTextColumn>
            <DataGridTextColumn Binding="{Binding Balance}" Header="Balance" Width="*" MaxWidth="200" MinWidth="100"></DataGridTextColumn>
            <DataGridTextColumn Binding="{Binding TimeUtc}" Header="Time UTC" Width="*" MaxWidth="200" MinWidth="100"></DataGridTextColumn>
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
